---
import { Image } from "astro:assets";
interface Props {
  src: string;
  alt: string;
}
const { src, alt } = Astro.props;
---

<div
  class="legacy-card group relative aspect-4/5 w-62.5 shrink-0 overflow-hidden backface-hidden sm:w-75 md:w-87.5"
>
  <Image
    src={src}
    alt={alt}
    width={380}
    height={475}
    format="webp"
    class="h-full w-full object-cover object-center grayscale-0 transition-all duration-500 ease-out md:grayscale md:group-hover:scale-105 md:group-hover:grayscale-0"
    loading="lazy"
    decoding="async"
  />

  <div
    class="absolute inset-0 bg-linear-to-t from-black/50 to-transparent opacity-100 transition-opacity duration-300 md:opacity-0 md:group-hover:opacity-100"
  >
  </div>
</div>
